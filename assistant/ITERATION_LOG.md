---
title: Agent 架构变更日志
type: agent-changelog
last_updated: 2026-06-11
---

# Agent 架构变更日志

## 加载链（上下游）

**上游（由谁加载本文件）：**
权威源。日常写入与周复盘时都可以追加。重大变更即时追加，不必等周复盘。

**同级联动：**
- `长期记忆.md` — 当前处境 + 跨周工作节点 + 周工作复盘走长期记忆，ITERATION_LOG 不记工作进展。
- `MEMORY/MEMORY_LOG.md` — MEMORY_LOG 只管记忆系统代谢，ITERATION_LOG 管所有架构级变更。两者互不重复。

---

## 关于此文件

**迭代日志**追踪 Agent 系统自身的有意义变更：系统架构、skill、hook、协议、区域 agent、身份层结构、文档大版本更新等。

不记录每日工作进展、跨周工作节点、记忆代谢条目、记忆周复盘或一般项目研究笔记。

---

## 变更类型

| 类型 | 说明 |
|---|---|
| 架构 | 系统结构变更 |
| 技能+ | 新技能创建 |
| 技能~ | 技能更新或废弃 |
| 优化 | 性能或体验改进 |
| 文档 | 文档更新（无代码变更） |
| 集成 | 外部工具连接 |

---

## 条目格式

条目格式权威源 = `storage-agent.md §ITERATION_LOG.md 一节`（三段式：摘要 / 改动 / 影响表；禁止字段与行数上限同源）。本文件不维护格式模板副本（v5.4 起防双源发散）。

---

版本真相以 `## 变更记录` 末条为准；头部不维护版本快照（防归档僵化）。

---

## 变更记录

> 从旧到新追加，最新条目在最底部。读尾部即最新。

（版本化变更条目在此从旧到新追加。第一条通常是 `v1.0.0` 系统初始化。）

---

### v6.2.1 · 2026-06-26 · 架构 · 顶层目录重编号 + ITERATION_LOG 归位

**摘要**：`Assistant/` 顶层目录按"主动打开干活的浏览顺序"重编号；ITERATION_LOG 从根目录迁入 Meta Zone（自指域文件归位）。

**改动**：
- Meta Zone → `02 Meta Zone`；Reading → `03 Reading`；Writing → `04 Writing`（骨架固定低位号，功能区平级排后）
- `ITERATION_LOG.md` 根目录 → `02 Meta Zone/`；同步建 `_archive/ITERATION_LOG_archive/`
- 引用网全域扫描更新（活跃文件 8 处，历史快照冻结不动）
- 三区域 agent frontmatter `last_updated` 同步

**影响**：区域 agent 加载链路径已更新；旧路径 grep 验证活跃文件零残留。

---

### v6.3.0 · 2026-06-27 · 架构 · semantic 退注入（记忆架构重构）

**摘要**：semantic_memory 从启动注入层降级为 dream 中间工作区；白天运行时世界模型底座缩减为 USER + SOUL + AGENTS.md §R 三件套。

**改动**：
- `00.记忆区_agent.md`：注入列删 semantic / 提取下限命中判准补"正向确认"格 / 前置过滤表 C轴新增分流维度 / "启动注入组"全改"活跃组" → v5.6.0
- `semantic_memory.md`：职责头 / 文件标题 / 6 条状态 / 冻结说明同步
- `AGENTS.md §B`：启动序列删 semantic 第3步 → 5步（原7步）
- `settings.json`：删 cat semantic hook
- `dream` / `weekly-review` / `week-sync` skill：措辞同步

**影响**：现存 semantic 条目原地保留标历史，毕业搬迁单独排期；PGH v6.0 主文档 §3.6/§缺失四/三层表"semantic 启动注入"滞后（挂"待同步"）。

---

### v6.4.0 · 2026-06-27 · 架构 · memory_agent §记忆系统结构新增 + 思考协议加深

**摘要**：memory_agent 新增"记忆系统结构"全景节（schema定义/信息流图/代谢对照基线/生命周期）；AGENTS.md §R 思考协议加深（触发范围扩展、执行遇新问题返回②）。

**改动**：
- `00.记忆区_agent.md`：总则块后新增 `## 记忆系统结构` 节 → v5.7.0
  - §schema是什么（预测单元本体两件 + 成立判据）
  - §信息流与分流（主干图：对话→工作库/记忆系统交叉；episodic双提取来源；progress两走向）
  - §代谢对照基线（差分减数 = 现有schema全集三层；semantic退注入后代谢期是其唯一被读取时机）
  - §生命周期（候选→终态全链+各段指针）
- `AGENTS.md §R`：触发范围扩展（协议是"遇到问题就启动的处理机"，非仅每轮开头）；执行遇新问题"返回③"改"返回②"（新问题=信息缺口，须先重检索再推导）

**影响**：dream skill 第3步代谢对照基线现有明确权威源；§R 变更属宪法层 C 级（已取 verdict）。

---

### v6.5.0 · 2026-06-27 · 架构 · 升格门重构（★★★再命中→怀疑触发器）

**摘要**：episodic→semantic 跨情景升格唯一门改为周日横向统合；★★★再命中从升格触发器重定义为怀疑触发器。

**改动**：
- `00.记忆区_agent.md §升星`："★★★ 过判准→升"改"跨情景★★★再命中→不直升、提取+1、转标`待统合簇`留周日判去向" → v5.8.0
- `00.记忆区_agent.md §横向统合`：冠"跨情景升格唯一门"定位句；聚类优先纳入`待统合簇`
- 信息流图同步更新两处（★★★→唯一门箭头）

**影响**：★★★堆 episodic 未升 = 系统正常工作（等周日统合判去向，非 bug）；频率倒挂修复（频率差匹配操作成本差，慢轨等一周不亏）。